extend frame
block content

    .middle-box.text-center.loginscreen.animated.fadeInDown(style='margin-top:0px; margin-bottom:100px; padding-top:0px;')
        div
            div
                h1.logo-name(style="position:relative; left:-30%; margin-bottom:25px;") odigós
            h3 Welcome to odigós delivery systems.
            p
                | Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vivamus et ullamcorper enim. Donec pharetra dui eu lorem feugiat dapibus.
            p
                | Login in. To see it in action.
            if message.length > 0
                .alert.alert-danger #{message}
            form.m-t(role='form', action='/attempt-login' method='post')
                .form-group
                    input.form-control(type='text', name='email', placeholder='Email Address', required='')
                .form-group
                    input.form-control(type='password', name='password', placeholder='Password', required='')
                button.btn.btn-primary.block.full-width.m-b(type='submit') Login
                hr
                a(href='/password-reset')
                    small Forgot password?
                p.text-muted.text-center
                    small Don't have an account?
                a.btn.btn-sm.btn-white.btn-block(href='/signup') Create Account
            p.m-t
                small NJS+ framework based on Bootstrap 3 © 2014
                
                
                
    db.tracking.find({contract: 555555})